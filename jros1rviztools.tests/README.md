Tests for `jros1rviztools` library.

# Run

Before starting tests make sure to run RViz:

```bash
rviz
```

Then add "rviz" - "Marker Array". This should subscribe RViz to "visualization_marker_array" topic which is used for testing.
